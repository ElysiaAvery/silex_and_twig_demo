<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <link rel="stylesheet" href="css/styles.css" media="screen" title="no title">
    <title>NASA</title>
  </head>
  <body>
    <div class="container">
      <h1>NASA</h1>
      {% if spacecrafts is not empty %}
        <h3>All of the current Spacecrafts:</h3>
        <ul>
          {% for spacecraft in spacecrafts %}
            <li>{{ spacecraft.getName }} is heading to {{ spacecraft.getDestination }}</li>
          {% endfor %}
        </ul>
      {% endif %}
      <form action="/new_spacecrafts" method="post">
        <div class="form-group">
          <label for="name">Enter Spacecraft's Name:</label>
          <input name="name" id="space_name" class="form-control">
        </div>
        <div class="form-group">
          <label for="destination">Enter Spacecraft's Destination:</label>
          <input name="destination" id="destination" class="form-control">
        </div>
        <button type='submit' class='btn btn-success'>Add Spacecraft</button>
      </form>
      <br>
      <form action="/delete_spacecrafts" method="post">
        <button type='submit' class='btn btn-danger'>Delete All Spacecrafts</button>
      </form>
      <p><a href='/'>Home</a></p>
    </div>
  </body>
</html>

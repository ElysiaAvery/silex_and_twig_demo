<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <link rel="stylesheet" href="css/styles.css" media="screen" title="no title">
    <title>NASA</title>
  </head>
  <body>
    <div class="container">
      <h1>NASA</h1>
      {% if astronauts is not empty %}
        <h3>All of the current Astronauts:</h3>
        <ul>
          {% for astronaut in astronauts %}
            <li>{{ astronaut.getName }}, {{ astronaut.getAge }} years old</li>
          {% endfor %}
        </ul>
      {% endif %}
      <form action="/astronauts" method="post">
        <div class="form-group">
          <label for="name">Enter Your Name:</label>
          <input name="name" id="name" class="form-control">
        </div>
        <div class="form-group">
          <label for="age">Enter Your Age:</label>
          <input name="age" id="age" class="form-control" type="number">
        </div>
        <button type='submit' class='btn btn-success'>Add Astronaut</button>
      </form>
      <br>
      <form action="/delete_astronauts" method="post">
        <button type='submit' class='btn btn-danger'>Delete All Astronauts</button>
      </form>
      <p><a href='/spacecrafts'>See All Spacecrafts...</a></p>
    </div>
  </body>
</html>
